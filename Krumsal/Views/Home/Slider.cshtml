@model IEnumerable<Kurumsal.Models.Sınıflar.Slider>
@{ Layout = null; }

<div class="hero-slider owl-carousel">
    @foreach (var item in Model)
    {
<div class="hero-slide-item set-bg" data-setbg="@item.ResimURL">

    <div class="hs-text">

        @*<div class="slider-text">
            <h2 class="hs-title-1">@Html.Raw(item.Baslik)</h2>

            <div class="slider-desc">
                @Html.Raw(item.Aciklama)
            </div>
        </div>*@

        @*<h2 class="hs-title-2"><span>from the best chefs</span></h2>
            <h2 class="hs-title-3"><span>for all the foodies</span></h2>*@
    </div>

</div>}
</div>
